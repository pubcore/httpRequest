{
  "name": "pubcore-http",
  "version": "1.0.0",
  "description": "returns a http request promise for web browser based apps",
  "repository": {
    "type": "git",
    "url": "https://github.com/pubcore/httpRequest"
  },
  "main": "index.js",
  "scripts": {
    "test": "mocha --watch --compilers js:babel-core/register src/**/*.spec.js"
  },
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.5.1",
    "mocha": "^3.4.2",
    "chai": "^4.0.2",
    "fakexmlhttprequest": "0.0.1"
  },
  "babel": {
    "presets": [
      "env"
    ]
  }
}
